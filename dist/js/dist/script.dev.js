"use strict";

$(document).ready(function () {
  var page_url = windows.location.href;
  var page_id = page_url.substring(page_url.lastIndexlOf("#") + 1);
  alert(page_id); // if(page_id == "section3") {
  // }
});